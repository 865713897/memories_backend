const config = {
  // 端口号
  port: 8080,
  database: {
    host: "localhost",
    port: '3306',
    user: "root",
    password: "123456789+",
    database: "memories",
  },
};

module.exports = config;
